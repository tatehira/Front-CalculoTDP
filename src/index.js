import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    {/* Remova ou altere o conteúdo dentro do componente App */}
    <div>
      {/* Coloque aqui o conteúdo do seu aplicativo */}
    </div>
  </React.StrictMode>,
);

reportWebVitals();
